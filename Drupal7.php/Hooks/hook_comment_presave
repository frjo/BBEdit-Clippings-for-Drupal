#INDENT#
/**
 * Implements hook_comment_presave().
 */
function #BASENAME#_comment_presave($comment) {
  // Remove leading & trailing spaces from the comment subject.
  $comment->subject = trim($comment->subject);
}
