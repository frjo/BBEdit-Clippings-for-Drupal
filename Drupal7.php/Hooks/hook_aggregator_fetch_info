#INDENT#
/**
 * Implements hook_aggregator_fetch_info().
 */
function #BASENAME#_aggregator_fetch_info() {
  return array(
    'title' => t('Default fetcher'),
    'description' => t('Default fetcher for resources available by URL.'),
  );
}
