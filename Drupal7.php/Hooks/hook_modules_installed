#INDENT#
/**
 * Implements hook_modules_installed().
 */
function #BASENAME#_modules_installed($modules) {
  if (in_array('lousy_module', $modules)) {
    variable_set('lousy_module_conflicting_variable', FALSE);
  }
}
