#INDENT#
/**
 * Implements hook_user_categories().
 */
function #BASENAME#_user_categories() {
  return array(array(
    'name' => 'account',
    'title' => t('Account settings'),
    'weight' => 1,
  ));
}
